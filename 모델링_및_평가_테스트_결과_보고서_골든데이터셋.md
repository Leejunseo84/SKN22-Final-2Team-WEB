# 모델링 및 평가 테스트 결과 보고서 (골든 데이터셋 기준)

## 1. 문서 개요
- **프로젝트명**: TailTalk AI
- **평가 대상**: FastAPI 기반 챗봇 응답 로직 및 추천 엔진
- **평가 기준**: 골든 데이터셋(V2) 기반 자동화 평가
- **작성 일자**: 2026-04-21
- **평가 요약**: 종합 정확도 **85.2%**로 통과(PASS) 기준 충족

## 2. 평가 목적
- 골든 데이터셋의 정답(Ground Truth)과 모델 응답 간의 일치도 검증
- 인텐트 분류(Intent), 검색 필터 추출(Filter), 펫 프로필 연동의 정확성 확인
- 추천 상품 리스트(Retrieval)의 재현율(Recall) 및 정밀도(Precision) 측정
- 응답 메시지 내 필수 키워드 포함 여부 검증

## 3. 평가 환경 및 데이터
- **LLM**: gpt-4o-mini
- **오케스트레이션**: LangGraph (TailTalk Chat Flow)
- **평가 데이터**: `ai_logic_golden_dataset_ids_only.json` (V2)
- **주요 지표**: Logic Precision, Retrieval F1-Score, Keyword Match Score

## 4. 종합 평가 결과 요약

| 지표 (Metric) | 평균 점수 (Average) | 상태 (Status) |
| :--- | :--- | :--- |
| **종합 정확도 (Total Accuracy)** | **85.2%** | **✅ PASS** |
| 로직 정밀도 (Logic Precision) | 83.8% | - |
| **추천 정확도 (Retrieval F1)** | **95.7%** | - |
| 키워드 매칭 (Keyword Match) | 76.5% | - |

> **판정 기준**: 종합 점수 85% 이상 시 적합으로 판정함.

## 5. 상세 테스트 결과 (주요 케이스)

| ID | 입력 요약 | 예상 경로 | 실제 경로 | 응답 요약 | 상품 매칭 | 판정 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| case_001 | 강아지 사료 추천해줘 | intent->profile->query->search->rerank->merge->respond | 동일 | 펫 프로필 기반 사료 추천 | 5/5 (100%) | Pass |
| case_002 | 간식도 같이 추천해줘 | intent->profile->query->search->rerank->merge->respond | 동일 | 이전 맥락 유지하며 간식 추천 | 5/5 (100%) | Pass |
| case_021 | 인기 있는 강아지 옷 | intent->profile->query->search->rerank->merge->respond | 동일 | 인기 키워드 반영 추천 | 0/5 (0%) | Fail |
| case_091 | 제일 싼 강아지 패드 | intent->profile->query->search->rerank->merge->respond | 동일 | 가격 필터(최저가) 반영 | 5/5 (100%) | Pass |
| case_092 | 제일 저렴한 거로 | intent->profile->query->search->rerank->merge->respond | 동일 | 정렬 기준 변경 적용 | 5/5 (100%) | Pass |

## 6. 결과 분석 및 시사점

### 6.1 강점 (Strengths)
- **추천 정확도(Retrieval F1: 95.7%)**: 골든 데이터셋에서 정의한 추천 상품 리스트와 실제 모델의 검색 결과가 매우 높은 일치율을 보임.
- **로직 안정성**: LangGraph의 노드 흐름이 대부분의 시나리오에서 예상대로 동작하며, 펫 프로필 및 필터 정보가 성공적으로 전달됨.

### 6.2 약점 및 개선 과제 (Weaknesses & Tasks)
- **키워드 매칭(76.5%)**: "인기", "최저가", "옷" 등 특정 상세 키워드가 최종 응답 메시지에서 누락되는 경우가 발생함. 응답 생성 프롬프트 보강 필요.
- **인기 품목 처리**: `case_021`과 같이 '인기 있는' 상품에 대한 추천 정확도가 낮게 측정됨. 인기도 기반 정렬 로직(Gold Layer 데이터 활용)의 고도화가 요구됨.
- **복합 필터 오류**: '3만원 이하'와 같은 한글 포함 가격 조건 처리 시 데이터 타입 에러가 발생할 가능성이 코드 분석 및 테스트 중 확인되었으므로 전처리 로직 수정 필요.

## 7. 종합 결론
본 TailTalk AI 시스템은 골든 데이터셋 기반 평가에서 85.2%의 점수를 기록하여 서비스 출시를 위한 최소 요구 성능을 확보한 것으로 판단됨. 특히 추천 엔진의 검색 성능(F1 Score)이 매우 우수하여 사용자에게 정확한 상품 정보를 제공할 수 있는 기반을 갖춤. 향후 응답 메시지의 세밀한 키워드 반영과 가격 필터 로직의 안정성 보강을 통해 완성도를 높일 것을 권장함.
